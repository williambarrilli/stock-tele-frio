import './styles.css';

export default function FormNewProduct() {
  return <></>;
}

{
  /* <Dialog
open={openModalNewProduct}
onClose={() => setOpenModalNewProduct(false)}
className="container-modal-new"
>
<div className="content-modal-new">ADD PRODUCT</div>
</Dialog>
<Dialog
open={sellProduct}
onClose={() => setSellProduct(false)}
className="container-modal-sell"
>
<div className="content-modal-sell">Sell product</div>
</Dialog> */
}
